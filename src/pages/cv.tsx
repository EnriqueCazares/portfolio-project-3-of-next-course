import React from 'react';
import BaseLayout from '@/components/layouts/BaseLayout';

type Props = {}

const cv = (props: Props) => {
  return (
    <BaseLayout>
        <div>i'm cv page</div>
    </BaseLayout>
  )
}

export default cv;